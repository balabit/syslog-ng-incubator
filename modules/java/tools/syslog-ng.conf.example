@version: 3.6
@module java

options {
  threaded(yes);
};

source s_tcp{
  tcp(
    port(5555)
  );
};

destination d_java{
  java(
    class_name("TestClass")
    class_path("/tmp")
    options(key(value))
  );
};

log{
  source(s_tcp);
  destination(d_java);
  flags(flow-control);
};

